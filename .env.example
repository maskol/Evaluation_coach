# Evaluation Coach Configuration Example
# Copy this file to .env and update with your values

# =============================================================================
# PRIMARY DATA SOURCE (Required)
# =============================================================================

# DL Webb App Server - Your lead-time database
LEADTIME_SERVER_URL=http://localhost:8000
LEADTIME_SERVER_ENABLED=true
LEADTIME_SERVER_TIMEOUT=30

# =============================================================================
# LLM CONFIGURATION (Required)
# =============================================================================

# OpenAI API Key for AI-powered insights
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview

# Optional: Local LLM via Ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama2

# =============================================================================
# DATABASE (Required)
# =============================================================================

DATABASE_URL=sqlite:///./data/evaluation_coach.db

# =============================================================================
# JIRA INTEGRATION (Optional - Not Currently Required)
# =============================================================================

# Jira can be integrated later via Model Context Protocol (MCP) if needed.
# Currently, all data comes from DL Webb App, so these are optional:

# JIRA_BASE_URL=https://your-company.atlassian.net
# JIRA_EMAIL=your-email@company.com
# JIRA_API_TOKEN=your-jira-api-token
# JIRA_VERIFY_SSL=true
# JIRA_ENABLED=false

# =============================================================================
# NOTES
# =============================================================================

# Current Setup:
# - All metrics come from DL Webb App (60K+ features, 14 ARTs, 24 PIs)
# - No Jira access required for core functionality
# - Jira integration can be added in the future without code changes
#
# To enable Jira later:
# 1. Set JIRA_ENABLED=true
# 2. Configure JIRA_BASE_URL, JIRA_EMAIL, JIRA_API_TOKEN
# 3. Optionally configure Jira MCP server in VS Code settings
